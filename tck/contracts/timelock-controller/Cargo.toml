[package]
name = "timelock-controller"
version = "0.1.0"
edition = "2021"

[dependencies]
tako-sdk.workspace = true

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
codegen-units = 1
strip = true

[lib]
crate-type = ["cdylib"]
