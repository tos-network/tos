[package]
name = "test-referral"
version = "0.1.0"
edition = "2021"
description = "Simple test contract for referral syscalls"

[dependencies]
tako-sdk.workspace = true

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
