# TOS Network - Environment Variables Example
# Copy this file to .env and modify the values as needed

# Git commit hash (auto-detected if not set)
TOS_COMMIT_HASH=

# Network configuration
# Use testnet by default for local/dev. Switch to mainnet only if needed.
TOS_NETWORK=testnet

# Logging level
RUST_LOG=info

# Miner configuration
TOS_MINER_ADDRESS=your_wallet_address_here
TOS_MINER_THREADS=4

# Docker registry (for pushing images)
DOCKER_REGISTRY=ghcr.io/tos-network

# Database configuration
TOS_DATA_DIR=/var/run/tos/data
TOS_CONFIG_DIR=/var/run/tos/config

# Bind addresses
TOS_RPC_BIND_ADDRESS=0.0.0.0:8080
TOS_P2P_BIND_ADDRESS=0.0.0.0:2125
# RPC stop timeout (seconds)
TOS_RPC_STOP_TIMEOUT_SECS=5

# Testnet peer configuration (from TESTNET_NODES.md)
# Seed node: 157.7.65.157:2125
# Validators: 157.7.78.65:2125, 157.7.192.216:2125
# Set either priority or exclusive nodes (leave empty if running seed node).
TOS_PRIORITY_NODES=157.7.65.157:2125,157.7.78.65:2125,157.7.192.216:2125
TOS_EXCLUSIVE_NODES=

# A2A escrow validation flags
A2A_ESCROW_VALIDATE_STATES=true
A2A_ESCROW_ALLOWED_STATES=created,funded,pending-release,challenged
A2A_ESCROW_VALIDATE_TIMEOUT=true
A2A_ESCROW_VALIDATE_AMOUNTS=false

# Performance tuning
CARGO_BUILD_JOBS=8
CARGO_TARGET_DIR=/tmp/cargo-target
